import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 300,
    paddingVertical: 15,
  },
  input: {
    color: "white",
    borderColor: "white",
    borderBottomWidth: 1,
    fontSize: 24,
    padding: 10,
    fontFamily: "Jost",
  },
});

export default styles;
